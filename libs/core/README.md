# core

Core. Elementos base de la aplicación que contienen configuración global y servicios compartidos entre todas las capas.

## Examples

    /guards
    /interceptors
    /services
    /config

## Running unit tests

Run `nx test core` to execute the unit tests.
