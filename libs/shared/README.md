# shared

Shared. Elementos reutilizables entre distintas features, no ligados a una funcionalidad concreta.

## Examples

    /utils
    /constants
    /pipes

## Running unit tests

Run `nx test shared` to execute the unit tests.
