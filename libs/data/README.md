# data

Data. Capa encargada de la gestión de datos de la aplicación, incluyendo acceso a APIs, mapeo de modelos y lógica de repositorios.

## Examples

    /repositories
    /models
    /adapters
    /datasources

## Running unit tests

Run `nx test data` to execute the unit tests.
